return {
  "nvim-tree/nvim-tree.lua",
  opts = {
    git = {
      enable = true, -- Set to true if you want git features, false otherwise
      ignore = false, -- Set to false to prevent NvimTree from ignoring files in .gitignore
      timeout = 500,
    },
  },
}
